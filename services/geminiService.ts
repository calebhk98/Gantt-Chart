// Service removed as per user request.
// import { GoogleGenAI, Type } from "@google/genai";
// import { GeneratedTask } from '../types';

export const generateProjectPlan = async (description: string): Promise<any[]> => {
  return [];
};
